<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:WPFSchedule">
    <Style x:Key="buttonTriangle">
        <Setter Property="Button.Height" Value="20px"/>
        <Setter Property="Button.Width" Value="20px"/>
        <Setter Property="Button.Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Polygon Fill="{TemplateBinding Background}" Points="0,10,20,20,20,0"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="date">
        <Setter Property="TextBlock.VerticalAlignment" Value="Center"/>
        <Setter Property="TextBlock.Margin" Value="10"/>
        <Setter Property="TextBlock.FontWeight" Value="Bold"/>
        <Setter Property="TextBlock.FontSize" Value="24px"/>
    </Style>

    <Style x:Key="hyphen">
        <Setter Property="TextBlock.VerticalAlignment" Value="Center"/>
        <Setter Property="TextBlock.FontWeight" Value="Bold"/>
        <Setter Property="TextBlock.FontSize" Value="24px"/>
    </Style>

    <Style x:Key="addSchedule">
        <Setter Property="Button.Width" Value="150px"/>
        <Setter Property="Button.Height" Value="40px"/>
        <Setter Property="Button.HorizontalAlignment" Value="Right"/>
        <Setter Property="Button.Content">
            <Setter.Value>
                <TextBlock Text="+ Add schedule" FontWeight="DemiBold" FontSize="16px"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Button.Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border CornerRadius="5" Background="{TemplateBinding Background}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" Color="LightGray" Direction="0" Opacity="0.5" ShadowDepth="0"/>
                            </Border.Effect>
                        </Border>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="dayOfWeek">
        <Setter Property="TextBlock.VerticalAlignment" Value="Center"/>
        <Setter Property="TextBlock.HorizontalAlignment" Value="Center"/>
        <Setter Property="TextBlock.FontSize" Value="30px"/>
    </Style>

    <Style x:Key="listOfSchedules">
        <Setter Property="ListBox.Margin" Value="5"/>
        <Setter Property="ListBox.BorderBrush" Value="White"/>
        <Setter Property="ListBox.ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" Margin="0,0,0,5">
                        <TextBlock Text="{Binding StringFormat=Id: {0}, Path=Id}"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding StringFormat={}{0:HH:mm}, Path=EventStart}"/>
                            <TextBlock Text=" - "/>
                            <TextBlock Text="{Binding StringFormat={}{0:HH:mm}, Path=EventFinish}"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="borderToColumns">
        <Setter Property="Border.BorderBrush" Value="LightGray"/>
        <Setter Property="Border.BorderThickness" Value="1, 0, 0, 0"/>
        <Setter Property="Border.Background" Value="{x:Null}"/>
    </Style>

    <Style x:Key="lastBorderToColumns">
        <Setter Property="Border.BorderBrush" Value="LightGray"/>
        <Setter Property="Border.BorderThickness" Value="1, 0, 1, 0"/>
        <Setter Property="Border.Background" Value="{x:Null}"/>
    </Style>

    <Style x:Key="borderToRows">
        <Setter Property="Border.BorderBrush" Value="LightGray"/>
        <Setter Property="Border.BorderThickness" Value="0, 0, 0, 1"/>
        <Setter Property="Border.Background" Value="{x:Null}"/>
    </Style>

    <Style x:Key="stackPanelWeek">
        <Setter Property="StackPanel.Height" Value="75"/>
        <Setter Property="StackPanel.HorizontalAlignment" Value="Center"/>
    </Style>
</ResourceDictionary>